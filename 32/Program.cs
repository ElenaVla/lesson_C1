// Задать массив из 8 элементов, заполненных нулями и единицами вывести их на экран
int[] array = {0, 1, 0, 0, 0, 1, 1, 1};
int i;
for (i=0; i<array.Length; i++)
{
    Console.Write(array[i]);
    Console.Write(" ");
}
